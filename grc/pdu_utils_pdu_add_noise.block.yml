# auto-generated by grc.converter

id: pdu_utils_pdu_add_noise
label: Add Noise
category: '[Sandia]/PDU Utilities/Debug Utils'

parameters:
-   id: level
    label: Noise Level (stddev)
    dtype: float
    default: '0.0'
-   id: offset
    label: Offset
    dtype: float
    default: '0.0'
-   id: scale
    label: Scale
    dtype: float
    default: '1.0'
-   id: seed
    label: Seed
    dtype: int
    default: '123456789'
-   id: dist
    label: Distribution
    dtype: int
    default: pdu_utils.UNIFORM
    options: [pdu_utils.UNIFORM, pdu_utils.GAUSSIAN]
    option_labels: [Uniform, Gaussian]

inputs:
-   domain: message
    id: pdu_in
    optional: true

outputs:
-   domain: message
    id: pdu_out
    optional: true
asserts:
- ${ level >= 0 }

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_add_noise(${level}, ${offset}, ${scale}, ${seed}, ${dist})
    callbacks:
    - set_noise_level(${level})
    - set_offset(${offset})
    - set_scale(${scale})
    - set_noise_dist(${dist})

file_format: 1

# auto-generated by grc.converter

id: pdu_utils_pdu_add_noise
label: Add Noise
category: '[Sandia]/PDU Utilities/Debug Utils'

parameters:
-   id: level
    label: Noise Level
    dtype: float
    default: '0.0'
-   id: offset
    label: Offset
    dtype: float
    default: '0.0'
-   id: scale
    label: Scale
    dtype: float
    default: '1.0'

inputs:
-   domain: message
    id: pdu_in
    optional: true

outputs:
-   domain: message
    id: pdu_out
    optional: true
asserts:
- ${ level >= 0 }

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_add_noise(${level}, ${offset}, ${scale})
    callbacks:
    - set_noise_level(${noise_level})
    - set_offset(${offset})
    - set_scale(${scale})

documentation: |-
    Adds randomly generated noise to u8, f32 or c32 PDUs, can also add an
    offset or scale factor. Noise is applied first, then scaling, then offset.

file_format: 1

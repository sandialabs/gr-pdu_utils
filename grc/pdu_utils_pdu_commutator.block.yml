# auto-generated by grc.converter

id: pdu_utils_pdu_commutator
label: PDU Commutator
category: '[Sandia]/PDU Utilities/'

parameters:
-   id: num_paths
    label: Num Paths
    dtype: int
    default: '2'
    hide: part

inputs:
-   domain: message
    id: pdu_in
    optional: true

outputs:
-   domain: message
    id: pdu_out_
    multiplicity: ${ num_paths }
    optional: true

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_commutator(${num_paths})

file_format: 1

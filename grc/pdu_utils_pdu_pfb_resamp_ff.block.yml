# auto-generated by grc.converter

id: pdu_utils_pdu_pfb_resamp_ff
label: PDU PFB Resampler
category: '[Sandia]/PDU Utilities'

parameters:
-   id: resamp_rate
    label: Resample Rate
    dtype: float
    default: '1'
-   id: taps
    label: PFB Taps
    dtype: float_vector
-   id: nfilts
    label: N Filters
    dtype: int
    default: '32'

inputs:
-   domain: message
    id: pdu_in

outputs:
-   domain: message
    id: pdu_out
    optional: true

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_pfb_resamp_ff(${taps}, ${nfilts}, ${resamp_rate})
    callbacks:
    - set_taps(${taps})

documentation: |-
    This block will apply a PFB resampling kernel to input data.

file_format: 1

# auto-generated by grc.converter

id: pdu_utils_pdu_fir_filter_fff
label: PDU FIR Filter
category: '[Sandia]/PDU Utilities'

parameters:
-   id: taps
    label: FIR Taps
    dtype: float_vector
    default: '1'

inputs:
-   domain: message
    id: pdu_in

outputs:
-   domain: message
    id: pdu_out
    optional: true

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_fir_filter_fff(${taps})
    callbacks:
    - set_taps(${taps})

documentation: |-
    This block will apply a FIR filter to an input data array

file_format: 1

# auto-generated by grc.converter

id: pdu_utils_pdu_clock_recovery
label: PDU Clock Recovery
category: '[Sandia]/PDU Utilities'

parameters:
-   id: binary_slice
    label: Slice?
    dtype: enum
    options: [binary, none]
    option_attributes:
        val: ['True', 'False']
-   id: debug
    label: Debug
    dtype: enum
    default: 'False'
    options: ['False', 'True']
    hide: part
-   id: win_type
    label: Window Type
    dtype: enum
    default: pdu_utils.TUKEY_WIN
    options: [pdu_utils.TUKEY_WIN, pdu_utils.GAUSSIAN_WIN]
    option_labels: [Tukey, Gaussian]    


inputs:
-   domain: message
    id: pdu_in

outputs:
-   domain: message
    id: pdu_out
-   domain: message
    id: debug
    optional: true
-   domain: message
    id: zeroX
    optional: true
-   domain: message
    id: window
    optional: true

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_clock_recovery(${binary_slice.val}, ${debug}, ${win_type})
    


file_format: 1

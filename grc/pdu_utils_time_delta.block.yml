# auto-generated by grc.converter

id: pdu_utils_time_delta
label: Time Delta
category: '[Sandia]/PDU Utilities'

parameters:
-   id: name
    label: Name
    dtype: string
    default: '''Time Printer'''

inputs:
-   domain: message
    id: pdu_in

templates:
    imports: import pdu_utils
    make: pdu_utils.time_delta(${name})

documentation: |-
    Block will attempt to access the 'wall_clock_time' PDU dictionary element and print the time delta.

file_format: 1

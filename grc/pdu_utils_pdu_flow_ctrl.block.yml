# auto-generated by grc.converter

id: pdu_utils_pdu_flow_ctrl
label: PDU Flow Controller
category: '[Sandia]/PDU Utilities'

parameters:
-   id: max_nmsgs
    label: Max Messages
    dtype: int
    default: '25'
-   id: nblocks
    label: Num Blocks
    dtype: int
    default: '1'
    hide: part
-   id: block0
    label: Block 0
    dtype: string
    hide: ${ ('none' if nblocks > 0 else 'all') }
-   id: block1
    label: Block 1
    dtype: string
    hide: ${ ('none' if nblocks > 1 else 'all') }
-   id: block2
    label: Block 2
    dtype: string
    hide: ${ ('none' if nblocks > 2 else 'all') }
-   id: block3
    label: Block 3
    dtype: string
    hide: ${ ('none' if nblocks > 3 else 'all') }
-   id: block4
    label: Block 4
    dtype: string
    hide: ${ ('none' if nblocks > 4 else 'all') }
-   id: block5
    label: Block 5
    dtype: string
    hide: ${ ('none' if nblocks > 5 else 'all') }
-   id: block6
    label: Block 6
    dtype: string
    hide: ${ ('none' if nblocks > 6 else 'all') }
-   id: block7
    label: Block 7
    dtype: string
    hide: ${ ('none' if nblocks > 7 else 'all') }
-   id: block8
    label: Block 8
    dtype: string
    hide: ${ ('none' if nblocks > 8 else 'all') }
-   id: block9
    label: Block 9
    dtype: string
    hide: ${ ('none' if nblocks > 9 else 'all') }
-   id: block10
    label: Block 10
    dtype: string
    hide: ${ ('none' if nblocks > 10 else 'all') }
-   id: block11
    label: Block 11
    dtype: string
    hide: ${ ('none' if nblocks > 11 else 'all') }
-   id: block12
    label: Block 12
    dtype: string
    hide: ${ ('none' if nblocks > 12 else 'all') }
-   id: block13
    label: Block 13
    dtype: string
    hide: ${ ('none' if nblocks > 13 else 'all') }
-   id: block14
    label: Block 14
    dtype: string
    hide: ${ ('none' if nblocks > 14 else 'all') }
-   id: verbose
    label: Verbose
    dtype: raw
    default: 'False'
    options: ['True', 'False']
    hide: part

inputs:
-   domain: message
    id: pdu_in

outputs:
-   domain: message
    id: pdu_out
asserts:
- ${ nblocks >= 1 and nblocks < 15 }
- ${ max_nmsgs >= 1 }

templates:
    imports: import pdu_utils
    make: |-
        <% block_ids = [block0, block1, block2, block3, block4, block5, block6, block7, block8, block9, block10, block11, block12, block13, block14][:nblocks] %>
        pdu_utils.pdu_flow_ctrl(self, ${block_ids}, ${max_nmsgs})

file_format: 1

# auto-generated by grc.converter

id: pdu_utils_pdu_align
label: PDU Align
category: '[Sandia]/PDU Utilities'

parameters:
-   id: syncwords
    label: Syncwords
    dtype: string
    default: 01010101010101010010110111010100, 10101010101010100010110111010100
-   id: offset
    label: Offset
    dtype: int
    default: '-16'
-   id: threshold
    label: Threshold
    dtype: int
    default: '0'
-   id: mode
    label: Output if no sync
    dtype: enum
    options: [pdu_utils.ALIGN_DROP, pdu_utils.ALIGN_FORWARD, pdu_utils.ALIGN_EMPTY]
    option_labels: [Send nothing, Send input PDU, Send empty PDU]
-   id: match_mode
    label: Sync match mode
    dtype: enum
    options: [pdu_utils.ALIGN_FIRST_MATCH, pdu_utils.ALIGN_BEST_MATCH]
    option_labels: [Output at first match, Look ahead for better match]

inputs:
-   domain: message
    id: pdu_in

outputs:
-   domain: message
    id: pdu_out
asserts:
- ${ threshold >= 0 }

templates:
    imports: from gnuradio import pdu_utils
    make: pdu_utils.pdu_align(${syncwords}, ${threshold}, ${offset}, ${mode}, ${match_mode})


file_format: 1

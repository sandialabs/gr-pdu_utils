# auto-generated by grc.converter

id: pdu_utils_msg_drop_random
label: Random Msg Drop
category: '[Sandia]/PDU Utilities/Debug Utils'

parameters:
-   id: p_drop
    label: P{drop}
    dtype: float
    default: '0.1'
-   id: seed
    label: Seed
    dtype: int
    default: '12345678'

inputs:
-   domain: message
    id: in
    optional: true

outputs:
-   domain: message
    id: out
    optional: true
asserts:
- ${ p_drop >= 0 }
- ${ p_drop <= 1 }

templates:
    imports: import pdu_utils
    make: pdu_utils.msg_drop_random(${p_drop}, ${seed})
    callbacks:
    - set_prob_drop(${p_drop})
  

file_format: 1

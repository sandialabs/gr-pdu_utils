# auto-generated by grc.converter

id: pdu_utils_pack_unpack
label: PDU Pack/Unpack
category: '[Sandia]/PDU Utilities'

parameters:
-   id: mode
    label: Mode
    dtype: enum
    options: [pdu_utils.MODE_PACK_BYTE, pdu_utils.MODE_UNPACK_BYTE, pdu_utils.MODE_BITSWAP_BYTE]
    option_labels: [Pack, Unpack, Bit Reverse]
-   id: bit_order
    label: Bit Order
    dtype: enum
    options: [pdu_utils.BIT_ORDER_MSB_FIRST, pdu_utils.BIT_ORDER_LSB_FIRST]
    option_labels: [MSB First, LSB First]

inputs:
-   domain: message
    id: pdu_in

outputs:
-   domain: message
    id: pdu_out
    optional: true

templates:
    imports: import pdu_utils
    make: pdu_utils.pack_unpack(${mode}, ${bit_order})

file_format: 1

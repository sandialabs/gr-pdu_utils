# auto-generated by grc.converter

id: pdu_utils_take_skip_to_pdu
label: Take/Skip To PDU
category: '[Sandia]/PDU Utilities'

parameters:
-   id: type
    label: PDU Type
    dtype: enum
    options: [c, f, s, b]
    option_labels: [Complex, Float, Short, Byte]
    option_attributes:
        input: [complex, float, short, byte]
        vec_type: [complex_vector, float_vector, int_vector, int_vector]
    hide: part
-   id: take
    label: Take
    dtype: int
    default: '1024'
-   id: skip
    label: Skip
    dtype: int
    default: '4096'

inputs:
-   domain: stream
    dtype: ${ type.input }

outputs:
-   domain: message
    id: pdu_out
    optional: true
asserts:
- ${ take >= 1 }
- ${ take <= pdu_utils.TAKESKIP_MAXIMUM_PDU_SIZE }
- ${ skip >= 0 }

templates:
    imports: import pdu_utils
    make: pdu_utils.take_skip_to_pdu_${type}(${take}, ${skip})
    callbacks:
    - set_take(${take})
    - set_skip(${skip})

file_format: 1

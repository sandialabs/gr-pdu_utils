# auto-generated by grc.converter

id: pdu_utils_pdu_split
label: PDU Split
category: '[Sandia]/PDU Utilities'

parameters:
-   id: pass_empty
    label: Empty
    dtype: enum
    options: ['False', 'True']
    option_labels: [Drop, Print]

inputs:
-   domain: message
    id: pdu_in

outputs:
-   domain: message
    id: dict
    optional: true
-   domain: message
    id: data
    optional: true

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_split(${pass_empty})

file_format: 1

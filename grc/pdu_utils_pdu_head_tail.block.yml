# auto-generated by grc.converter

id: pdu_utils_pdu_head_tail
label: PDU Head/Tail
category: '[Sandia]/PDU Utilities/Debug Utils'

parameters:
-   id: pdu_type
    label: PDU Type
    dtype: enum
    options: [pdu_utils.INPUTTYPE_UNPACKED_BYTE, pdu_utils.INPUTTYPE_PACKED_BYTE,
        pdu_utils.INPUTTYPE_FLOAT]
    option_labels: [U8 Bits, U8 Bytes, Float]
-   id: length
    label: Length
    dtype: int
    default: 8*8
-   id: histsize
    label: Hist Length
    dtype: int
    default: '64'

inputs:
-   domain: message
    id: pdu_in
    optional: true

outputs:
-   domain: message
    id: head
    optional: true
-   domain: message
    id: tail
    optional: true
asserts:
- ${ length >= 2 }
- ${ length <= 4096 }
- ${ histsize >= 2 }
- ${ histsize <= 4096 }

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_head_tail(${pdu_type}, ${length}, ${histsize})
    callbacks:
    - set_length(${length})
    - set_histsize(${histsize})


file_format: 1

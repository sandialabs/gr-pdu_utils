# auto-generated by grc.converter

id: pdu_utils_upsample
label: PDU Upsample
category: '[Sandia]/PDU Utilities'

parameters:
-   id: repeat
    label: Mode
    dtype: bool
    options: ['True', 'False']
    option_labels: [Repeat, Zero Pad]
-   id: n
    label: N
    dtype: int
    default: '4'

inputs:
-   domain: message
    id: pdu_in

outputs:
-   domain: message
    id: pdu_out
    optional: true
asserts:
- ${ n >= 1 }

templates:
    imports: import pdu_utils
    make: pdu_utils.upsample(${n}, ${repeat})
    callbacks:
    - set_n(${n})
    - set_repeat(${repeat})

file_format: 1

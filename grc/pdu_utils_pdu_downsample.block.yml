# auto-generated by grc.converter

id: pdu_utils_pdu_downsample
label: PDU Downsample
category: '[Sandia]/PDU Utilities/'

parameters:
-   id: decimation
    label: Decimation
    dtype: int
    default: '2'
-   id: phase
    label: Phase
    dtype: int
    default: '0'

inputs:
-   domain: message
    id: pdu_in
    optional: true

outputs:
-   domain: message
    id: pdu_out
    optional: true

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_downsample(${decimation}, ${phase})

file_format: 1

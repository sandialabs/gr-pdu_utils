# auto-generated by grc.converter

id: pdu_utils_pdu_length_filter
label: PDU Length Filter
category: '[Sandia]/PDU Utilities'

parameters:
-   id: length
    label: Length
    dtype: int
    default: '1024'
-   id: drop_long
    label: Mode
    dtype: enum
    options: ['True', 'False']
    option_labels: [Drop Longer, Drop Shorter]

inputs:
-   domain: message
    id: pdu_in
    optional: true

outputs:
-   domain: message
    id: pdu_out
    optional: true

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_length_filter(${length}, ${drop_long})


file_format: 1

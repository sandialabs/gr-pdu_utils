# auto-generated by grc.converter

id: pdu_utils_message_keep_1_in_n
label: Keep 1 in N
category: '[Sandia]/PDU Utilities'

parameters:
-   id: n
    label: N
    dtype: int

inputs:
-   domain: message
    id: in
    optional: true

outputs:
-   domain: message
    id: out
    optional: true
asserts:
- ${ n >= 1 }

templates:
    imports: import pdu_utils
    make: pdu_utils.message_keep_1_in_n(${n})
    callbacks:
    - set_n(${n})

documentation: |-
    This block is equivalent to the Stream keep_1_in_n block but for messages.

file_format: 1

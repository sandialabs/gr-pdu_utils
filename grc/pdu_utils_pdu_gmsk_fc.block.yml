# auto-generated by grc.converter

id: pdu_utils_pdu_gmsk_fc
label: PDU GMSK
category: '[Sandia]/PDU Utilities'

parameters:
-   id: sensitivity
    label: Sensitivity
    dtype: float
-   id: taps
    label: FIR Taps
    dtype: float_vector
    default: '1'

inputs:
-   domain: message
    id: pdu_in

outputs:
-   domain: message
    id: pdu_out
    optional: true
asserts:
- ${ sensitivity > 0 }

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_gmsk_fc(${sensitivity}, ${taps})
    callbacks:
    - set_sensitivity(${sensitivity})
    - set_taps(${taps})

file_format: 1

# auto-generated by grc.converter

id: pdu_utils_pdu_set_m
label: PDU Set
category: '[Sandia]/PDU Utilities'

parameters:
-   id: k
    label: Key
    dtype: raw
    default: pmt.intern("key")
-   id: v
    label: Value
    dtype: raw
    default: pmt.intern("value")
-   id: kv_merge
    label: Parse for Value Insertion
    dtype: bool
    default: False
-   id: v_overwrite
    label: Value Overwrite
    dtype: bool
    default: False
    hide: part


inputs:
-   domain: message
    id: pdu_in
    optional: true

outputs:
-   domain: message
    id: pdu_out
    optional: true

templates:
    imports: from gnuradio import pdu_utils
    make: pdu_utils.pdu_set_m(${k}, ${v}, ${kv_merge}, ${v_overwrite})
    callbacks:
    - set_key(${k})
    - set_val(${v})
    - set_val(${kv_merge})
    - set_val(${v_overwrite})

file_format: 1

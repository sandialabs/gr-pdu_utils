# auto-generated by grc.converter

id: pdu_utils_pdu_set_m
label: PDU Set
category: '[Sandia]/PDU Utilities'

parameters:
-   id: k
    label: Key
    dtype: raw
    default: pmt.intern("key")
-   id: v
    label: Value
    dtype: raw
    default: pmt.intern("value")

inputs:
-   domain: message
    id: pdu_in
    optional: true

outputs:
-   domain: message
    id: pdu_out
    optional: true

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_set_m(${k}, ${v})
    callbacks:
    - set_key(${k})
    - set_val(${v})

documentation: |-
    This block is identical to the in-tree PDU Set block except it has callbacks for dynamically updating the KV pair.
    This minor change should be PR'd in tree.

file_format: 1

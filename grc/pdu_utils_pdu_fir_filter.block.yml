# auto-generated by grc.converter

id: pdu_utils_pdu_fir_filter
label: PDU FIR Filter
category: '[Sandia]/PDU Utilities'

parameters:
-   id: decimation
    label: Decimation
    dtype: int
    default: '1'
-   id: taps
    label: FIR Taps
    dtype: float_vector
    default: '1'

inputs:
-   domain: message
    id: pdu_in

outputs:
-   domain: message
    id: pdu_out
    optional: true

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_fir_filter(${decimation}, ${taps})
    callbacks:
    - set_taps(${taps})
    - set_decimation(${decimation})

file_format: 1

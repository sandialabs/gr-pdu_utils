# auto-generated by grc.converter

id: pdu_utils_pdu_binary_tools
label: PDU Binary Tools
category: '[Sandia]/PDU Utilities/'

parameters:
-   id: mode
    label: Mode
    dtype: enum
    options: ['0', '1', '2', '3', '4','5','6']
    option_labels: [Bit Flip, To NRZ, From NRZ, Slice, Endian Swap, Manchester Encode, Manchester Decode]

inputs:
-   domain: message
    id: pdu_in
    optional: true

outputs:
-   domain: message
    id: pdu_out
    optional: true

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_binary_tools(${mode})

file_format: 1

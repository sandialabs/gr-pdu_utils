# auto-generated by grc.converter

id: pdu_utils_extract_metadata
label: Extract Metadata
category: '[Sandia]/PDU Utilities'

parameters:
-   id: key
    label: Dictionary Key
    dtype: raw
    default: pmt.intern("")
-   id: scale
    label: Scale
    dtype: float
    default: '1.0'
    hide: part
-   id: offset
    label: Offset
    dtype: float
    default: '0.0'
    hide: part

inputs:
-   domain: message
    id: dict
    optional: true

outputs:
-   domain: message
    id: msg
    optional: true

templates:
    imports: |-
        import pdu_utils
        import pmt
    make: pdu_utils.extract_metadata(${key}, ${scale}, ${offset})
    callbacks:
    - set_key(${key})
    - set_scale(${scale})
    - set_offset(${offset})

file_format: 1

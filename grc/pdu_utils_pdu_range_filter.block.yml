# auto-generated by grc.converter

id: pdu_utils_pdu_range_filter
label: PDU Range Filter
category: '[Sandia]/PDU Utilities'

parameters:
-   id: key
    label: Key
    dtype: raw
    default: pmt.intern("key")
-   id: min
    label: Min Value
    dtype: real
-   id: max
    label: Max Value
    dtype: real
-   id: invert
    label: Invert Filter
    dtype: bool
    default: 'False'
    options: ['False', 'True']
    option_labels: ['No', 'Yes']

inputs:
-   domain: message
    id: pdu_in

outputs:
-   domain: message
    id: pdu_out

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_range_filter(${key}, ${min}, ${max}, ${invert})
    callbacks:
    - set_key(${key})
    - set_min(${min})
    - set_max(${max})
    - set_inversion(${invert})

file_format: 1

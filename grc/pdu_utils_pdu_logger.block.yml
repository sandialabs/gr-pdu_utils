# auto-generated by grc.converter

id: pdu_utils_pdu_logger
label: PDU Logger
category: '[Sandia]/PDU Utilities'

parameters:
-   id: logfile
    label: Logfile
    dtype: string
    default: '''/tmp/bursts/raw-'''

inputs:
-   domain: message
    id: pdu_in

templates:
    imports: import pdu_utils
    make: pdu_utils.pdu_logger(${logfile})

documentation: |-
    The PDU Logger block dumps a c32 or f32 data vector from PDUs to file.

file_format: 1

# auto-generated by grc.converter

id: pdu_utils_pdu_logger
label: PDU Logger
category: '[Sandia]/PDU Utilities'

parameters:
-   id: logfile
    label: Logfile
    dtype: string
    default: '''/tmp/bursts/raw-'''

-   id: pdus_per_file
    label: PDUs per File
    dtype: int
    default: '1'

inputs:
-   domain: message
    id: pdu_in

templates:
    imports: from gnuradio import pdu_utils
    make: pdu_utils.pdu_logger(${logfile}, ${pdus_per_file})

file_format: 1
